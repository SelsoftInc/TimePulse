# Encryption Configuration for TimePulse Frontend
# Copy this to your .env.local file and set a secure encryption key

# Encryption key for decrypting authentication and OAuth responses
# IMPORTANT: This key must match the ENCRYPTION_KEY in the backend
# Use a strong, random key in production (minimum 32 characters)
NEXT_PUBLIC_ENCRYPTION_KEY=timepulse-default-encryption-key-2024

# Example of a strong encryption key (DO NOT USE THIS IN PRODUCTION):
# NEXT_PUBLIC_ENCRYPTION_KEY=Tp$2024!Secure#Key@Auth&OAuth%Encrypt^Data*Protection

# Note: The NEXT_PUBLIC_ prefix makes this variable accessible in the browser
# Ensure this matches the backend ENCRYPTION_KEY exactly
