# Encryption Configuration for TimePulse Backend
# Copy this to your .env file and set a secure encryption key

# Encryption key for authentication and OAuth responses
# IMPORTANT: This key must match the NEXT_PUBLIC_ENCRYPTION_KEY in the frontend
# Use a strong, random key in production (minimum 32 characters)
ENCRYPTION_KEY=timepulse-default-encryption-key-2024

# Example of a strong encryption key (DO NOT USE THIS IN PRODUCTION):
# ENCRYPTION_KEY=Tp$2024!Secure#Key@Auth&OAuth%Encrypt^Data*Protection

# To generate a secure random key, you can use:
# Node.js: require('crypto').randomBytes(32).toString('hex')
# OpenSSL: openssl rand -hex 32
